const url = 'http://47.99.68.224:3000'

// 服务器链接配置
export default {
  // 用户管理模块
  user: `${url}/user`,
  // 验证码模块
  captcha: `${url}/captcha`,
  // 标签模块
  label: `${url}/label`,
  // 分类模块
  sort: `${url}/sort`,
  // 评论模块
  comment: `${url}/comment`,
  // 博文模块
  article: `${url}/article`

}
